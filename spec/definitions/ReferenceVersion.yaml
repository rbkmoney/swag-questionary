type: object
allOf:
  - $ref: '#/definitions/Reference'
  - type: object
    required:
      - version
    properties:
      version:
        description: Номер версии
        type: integer
        format: int64
        minimum: 1
        default: 1
